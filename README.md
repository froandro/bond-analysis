# Облигационный Арбитраж

[![ru](https://img.shields.io/badge/lang-ru-blue.svg)](README.ru.md)
[![en](https://img.shields.io/badge/lang-en-red.svg)](README.en.md)

Профессиональный калькулятор для расчёта доходности облигаций. Данные подтягиваются напрямую с Московской биржи (MOEX).

## Зачем это?

Вы покупаете облигацию и хотите понять реальную доходность. Не текущую (купон/цена), а **чистую** — с учётом налогов, комиссий, НКД и времени до погашения. Этот калькулятор считает:

- **YTM** — доходность к погашению с реинвестированием купонов
- **Net Yield** — то же самое, но после вычета налогов
- **Срок окупаемости** — когда купоны покроют премию
- **Общую прибыль** за всё время
- **График** денежных потоков с временной шкалой, привязанной к событиям (купон, оферта, колл, пут, окупаемость)
- **Детальную таблицу** каждой выплаты с офертой/колл/пут/окупаемостью
- **Сравнение облигаций** — добавляй несколько бумаг и сравнивай метрики рядом

## Скриншоты

![Главный экран](screenshots/183037.png?raw=true)
![Результаты расчёта](screenshots/183103.png?raw=true)

## Как пользоваться

### 1. Найти облигацию

Введи тикер, ISIN или название в поиск. Калькулятор найдёт её на MOEX и подставит цену, НКД, дату погашения и расписание купонов.
Поддерживаются ОФЗ, корпоративные облигации, еврооблигации и замещающие облигации.

### 2. Ввести свои параметры

- **Объём инвестиций** — сколько денег выделяешь на покупку
- **Налог** — 13% для резидентов (или 0% для ИИС-Б; для ОФЗ выставляется автоматически)
- **Комиссия** — обычно 0.01–0.1% у брокера

Остальное (цена, номинал, НКД, даты) подставится автоматически.

### 3. Изучить результат

Справа появятся:

- **Чистая доходность (NET)** — главный показатель
- **YTM** — доходность до погашения
- **Окупаемость** — когда премия окупится
- **Количество облигаций** — сколько влезет в бюджет
- **График** — как растёт капитал
- **Таблица** — каждая выплата с разбивкой
- **Кнопка PDF** — сохранить отчёт

### 4. Сравнить облигации

Нажми на иконку `BarChart3` в шапке — откроется таблица сравнения. Добавляй найденные облигации кнопкой `+` на боковой панели. Таблица покажет до 13 метрик рядом: цена, купон, YTM, NET, окупаемость и прибыль. В режиме сравнения печатается только таблица сравнения.

## Деплой на VPS

Инструкция для тех, кто впервые настраивает VPS. Всё делается через терминал (SSH).

### Шаг 1. Подключиться к VPS

```bash
ssh root@ip-адрес-вашего-сервера
```

Введи пароль, который дал хостинг-провайдер.

### Шаг 2. Установить Node.js

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
```

Проверь, что установилось:

```bash
node -v   # должно показать v20.x.x
npm -v    # должно показать 10.x.x
```

### Шаг 3. Забрать проект с GitHub

```bash
git clone https://github.com/froandro/bond-analysis.git
cd bond-analysis
```

### Шаг 4. Установить зависимости и собрать

```bash
npm install
npm run build
```

Готовый сайт появится в папке `dist`.

### Шаг 5. Запустить (навсегда)

```bash
npm install -g pm2
pm2 serve dist 3000 --name bond-analysis
pm2 save
pm2 startup
```

Команда `pm2 startup` покажет ещё одну команду — её тоже нужно выполнить (она добавляет автозапуск при перезагрузке сервера).

Сайт будет доступен по адресу `http://ip-адрес-вашего-сервера:3000`.

### Шаг 6. Подключить домен и HTTPS (рекомендуется)

```bash
sudo apt install -y nginx
```

Создай файл конфигурации:

```bash
sudo nano /etc/nginx/sites-available/bond-analysis
```

Вставь туда (замени `your-domain.com` на свой домен):

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

Включи сайт и перезагрузи Nginx:

```bash
sudo ln -s /etc/nginx/sites-available/bond-analysis /etc/nginx/sites-enabled/
sudo nginx -t          # проверить конфиг
sudo systemctl reload nginx
```

Теперь сайт работает на 80-м порту — можно открывать по домену без указания порта.

**HTTPS за 2 минуты (Let's Encrypt):**

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

Введи email, согласись с условиями — и всё, сайт на HTTPS.

### Если что-то пошло не так

Проверь, что порты 80 и 3000 открыты в фаерволле твоего хостинга (обычно это личный кабинет провайдера → файрволл или security group).

## Запуск локально (на своём ПК)

Если хочешь просто посмотреть, как работает:

```bash
git clone https://github.com/froandro/bond-analysis.git
cd bond-analysis
npm install
npm run dev
```

Открой в браузере http://localhost:3000.

## Технологии

React 19 · TypeScript · Vite · Tailwind CSS 4 · Recharts · Zod · MOEX ISS API · CBR API

## Лицензия

Apache 2.0
